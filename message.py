
class Message:
	id = 1
	def __init__(self, title, content, timestamp, added_by, notify_type, received_users):
		self.__message_title = title
		self.__message_id = id
		self.__content = content
		self.__timestamp = timestamp
		self.__added_by = added_by
		self.__notify_type = notify_type
		self.__received_users = received_users
		id += 1 
	def method(self):
		pass

